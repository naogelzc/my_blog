const getters = {
  lang: (state) => state.lang.lang,
  loaded: (state) => state.loaded.loaded,
  positionX: (state) => state.scroll.positionX,
  positionY: (state) => state.scroll.positionX,
}
export default getters
